<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://java.sun.com/xml/ns/javaee" xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd" id="WebApp_ID" version="3.0">
	<!-- <servlet>브라우저에서 요청하는 매핑 이름에 대해 실제로 실행하는 서블릿 클래스를 설정하는 태그
		<servlet-name>aaa</servlet-name>
		<servlet-class>sec01.ex01.FirstServlet</servlet-class>요청하는 매핑이름에 대해 기능을 수행하는 서블릿 클래스의 위치
	</servlet>
	<servlet>브라우저에서 요청하는 매핑 이름에 대해 실제로 실행하는 서블릿 클래스를 설정하는 태그
		<servlet-name>bbb</servlet-name>
		<servlet-class>sec01.ex01.SecondServlet</servlet-class>요청하는 매핑이름에 대해 기능을 수행하는 서블릿 클래스의 위치
	</servlet>
	<servlet-mapping>servlet태그와 이름값이 같은 것이 연결된다
		<servlet-name>aaa</servlet-name>
		<url-pattern>/first</url-pattern>기존 주소 대신 사용할 논리적인 서블릿 이름
	</servlet-mapping>
	<servlet-mapping>servlet태그와 이름값이 같은 것이 연결된다
		<servlet-name>bbb</servlet-name>
		<url-pattern>/second</url-pattern>기존 주소 대신 사용할 논리적인 서블릿 이름
	</servlet-mapping> -->
</web-app>